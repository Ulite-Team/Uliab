[package]
name = "ulb-plugin-sdk"
description = "Single source of truth for the plugin ABI (WIT interface and version constants)"
edition.workspace = true
version.workspace = true
license = "MIT"

[lints]
workspace = true

[dependencies]
ulb-plugin-sdk-macros = { path = "../ulb-plugin-sdk-macros" }

[dev-dependencies]
serde_json = "1"
