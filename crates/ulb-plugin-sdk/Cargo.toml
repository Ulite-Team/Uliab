[package]
name = "ulb-plugin-sdk"
description = "Single source of truth for the plugin ABI (WIT interface and version constants)"
edition.workspace = true
version.workspace = true
license = "MIT"

[dependencies]
ulb-config-macros = { path = "../ulb-config-macros" }

[lints]
workspace = true
